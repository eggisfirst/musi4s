//按钮颜色
export enum BtnTypes {
  Blue,
  Red
}

//退回/受理
export enum BtnTitle {
  applying,
  sendBack,
  null
}

//alert框按钮 确定/退回/取消
export enum AlertBtnTypes {
  comfirm,
  sendBack,
  cancle,
}

//星级认证状态
export enum StarCheckTypes {
  wait_handle,
  wait_reception,
  wait_sponsor,
  processing_record
}

